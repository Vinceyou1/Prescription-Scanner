## Hello!
This is a web app I created to scan prescription labels and automatically generate a medication schedule.

It uses Tesseract OCR to extract text from labels, which is sent to an LLM to create a structured JSON output.
